# ARM64 加载 ARM32 Bytecode 变更日志（防回归）

最后更新：2026-05-05 15:08
维护规则：每次改 `tolua.c` 或重编插件后，必须追加一条记录并更新回归矩阵；提交前必须执行 `tools/check_arm64_fr2_log.ps1`。

## 1. 当前目标

- 目标：在 `arm64` 上稳定加载 `arm32` LuaJIT bytecode。
- 当前主线问题：
1. `jygame/battle.lua`：`tmp.lua:2556 field or property Equipment does not exist`、`tmp.lua:2571 invalid arguments to method GetEquipment`（定位为 `CALL(C=2/3/4)` 在 FR2 下参数窗口右移导致 self/arg 错位）。

## 2. 变更记录（按时间倒序）

| 日期 | Commit | 变更摘要 | 目标问题 | 当前结论 |
|---|---|---|---|---|
| 2026-05-05 | `待提交` | 修复 `CALL(C=2)` 的通用 fast-path 漏判：仅收窄“零显式参数的 method-self forwarding”形态，命中 `MOV self<-obj; TGET* func<-obj[method]; CALL(C=2)` 时不再按 direct passthrough 跳过 FR2 重排，避免把 `self` 留在 FR1 的 `A+1` 槽位。同步补强离线工具：`tools/run_arm64_offline_regression.ps1` 新增 `proto132` 两组 `Set_needRefresh` 静态门禁，固定检查修后窗口 `MOV A9/A18; TGETS ... D=372; CALL(C=2)` | 用户新报错：`BATTLE_BeforeRoleAction -> tmp.lua:2915 bad argument #1 to Set_needRefresh (JyGame.BattleSprite expected, got string)` | 根因不是 `proto132` 局部缺补丁，而是通用 `CALL(C=2)` 快路径把 method-call 当成直传调用跳过了 FR2 空槽，导致 `currentSprite:Set_needRefresh()` 的 `self` 仍落在 `A+1`。修前当前产物两处坏窗口分别是 `line2915 MOV A8; TGETS A7 D=372; CALL A7 C2` 和 `line2986 MOV A17; TGETS A16 D=372; CALL A16 C2`；修后对齐为 `line2915 MOV A9; TGETS A7 D=372; CALL A7 C2` 与 `line2986 MOV A18; TGETS A16 D=372; CALL A16 C2`。完整回归 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto132_needrefresh_rootfix_v2 -WslDistro Ubuntu-24.04` 通过，`summary.json` 中 `battle.lua` 保持 `beforeroleaction_fail=0` 且其它模块门禁不回归；本次已改 `tolua_fr1_to_fr2.c` / `tolua.c`，需重编并同步 `so`，so 不提交 |
| 2026-05-05 | `待提交` | 修复 `AttackLogic_extendTalents2` 在 `proto320` 的又一组残留 `bf.Log` self 窗口，只按 `line6363` 的详细指令形态补丁：`MOV A12<-A3; TGETS A11 B=3 C=26 D=794; TGETS A13 B=1 C=27 D=283; TGETS A13 B=13 C=4 D=3332; KSTR A14 D=40; CAT A13..A14; CALL A11 C3`，右移为 GC64/FR2 的 `MOV A13<-A3; TGETS A14 B=1 C=27 D=283; TGETS A14 B=14 C=4 D=3588; KSTR A15 D=40; CAT A14..A15; CALL A11 C3`，把 method self/message 从 `A12/A13` 修正为 `A13/A14`。同步补强离线工具：`tools/run_arm64_offline_regression.ps1` 的 `Test-AttackLogicExtendTalents2TalentLogWindows` 新增 `proto320 line6363` 静态门禁，防止同类窗口再次漏检 | 用户新报错：`AttackLogic_extendTalents2 -> tmp.lua:6363 bad argument #1 to 'Log' (JyGame.BattleField expected, got string)` | 当前转换产物 `tmp/attacklogic_curr.proto320.dis.txt` 直接定位出残留坏窗口：`line6363 MOV A12 / TGETS A11 / TGETS A13 / TGETS A13 / KSTR A14 / CAT A13..A14 / CALL A11`，说明这是一组“函数槽已对，但 self/message 仍左偏一格”的半修状态。补丁后 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto320_line6363_fix -WslDistro Ubuntu-24.04` 通过，`summary.json` 中 `AttackLogic.lua` 保持 `extendtalents2_log_fail=0`；关键反汇编 `attacklogic.proto320.extendtalents2_talentlog_line6363.dis.txt` 已对齐为 `line6363 MOV A13 / TGETS A14 / TGETS A14 / KSTR A15 / CAT A14..A15 / CALL A11`；本次已改 `tolua_fr1_to_fr2.c` / `tolua.c`，需重编并同步 `so`，so 不提交 |
| 2026-05-05 | `待提交` | 修复 `buff.lua` 的 `BUFF_OnRoundBuff` 同一条 poison 分支上两个残留 FR1 窗口，并补强离线门禁。1) 在 [tolua_fr1_to_fr2.c](E:/Games/work/tolua_runtime_master/tolua_fr1_to_fr2.c) 只按 `proto0` 一组详细指令形态补丁 `GGET A5; TGETS A5 C30; TGETS A6 B3 C6; MULVN A6 B6 C17; GGET A7; TGETS A7 B7 C47; MOV A8<-A2; KSHORT A9; CALL A7 C3; MULVV A6 B6 C7; MOV A7<-A6; CALL A5 C2`，右移为 GC64/FR2 的 `A7/A8/A10/A11` 链，并同步修正后续 `MULVV/CALL`；2) 再只按 `proto0` 一组详细指令形态补丁 `TGETS A6 B0 C1; TGETS A6 B6 C20; MOV A7<-A6; TGETS A6 B6 C21; KSTR A8; TGETS A9; KSTR A10; MOV A11; KSTR A12; CAT A8..A12; CALL A6 C3`，右移为 `MOV A8<-A6; KSTR A9; TGETS A10; KSTR A11; MOV A12; KSTR A13; CAT A9..A13; CALL A6 C3`，修正 `bf:Log` 的 self/message 槽位。同步补强离线工具：`tools/run_arm64_offline_regression.ps1` 的 `Test-BuffOnRoundBuffMaxCallShape` 新增 `math.max` 和 `bf.Log` 两个精确静态窗口；`tools/bcrun_cli.c` 的 `buff_onroundbuff_maxcall` harness 现在会在 `math.max`/`bf.Log` 失败时带出当前 Lua 行号 | 用户新报错：`BUFF_OnRoundBuff -> tmp.lua:199 bad argument #2 to max (number expected, got function)` | 先用新 harness 证明旧 gate 盯错了 `line199` 直调窗口，真实坏点是同一 poison 分支更早的 `math.max` 链；修完后 harness 又继续暴露同分支后段 `bf:Log self` 左偏，说明之前离线工具还不够细。两组窗口补齐后，`powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_buff_onroundbuff_fixed2 -WslDistro Ubuntu-24.04` 通过，`summary.json` 中 `buff.lua` 为 `onroundbuff_fail=0`；`buff.onroundbuff_maxcall.log` 为 `run ok`；转换日志精确命中 `proto=0 pc=549/618 apply proto0 BUFF_OnRoundBuff poison ceil/max chain fix...` 和 `proto=0 pc=605 apply proto0 BUFF_OnRoundBuff poison bf.Log self fix...`；本次已改 `tolua_fr1_to_fr2.c` / `tolua.c`，需重编并同步 `so`，so 不提交 |
| 2026-05-05 | `待提交` | 继续收敛 `BUFF_OnRoundBuff` 的“属性毒族” FR1->FR2 残留窗口，只按 `proto0` 详细指令形态修 5 组同源模板：1) `虫毒/蛇毒/蟾毒` 共用的 attribute poison prechain，按三处字段常量成组匹配（`13/89/93`）把 `A6..A11` 右移到 `A7..A14`；2) `虫毒/蛇毒` 共用的 attribute late-poison producer，把 `A7/A8` 混合链收敛到 `A8/A9`；3) `虫毒/蛇毒` 共用的 insect `bf.Log` self 窗口，把 `A8..A13` 右移到 `A9..A14`；4) `蟾毒` 的 short `bf.Log` 窗口，把 `A9..A12` 右移到 `A10..A13`；5) `蟾毒` 的 long `bf.Log` 窗口，把 `CAT A9..A15` 收敛到 GC64/FR2 的 `CAT A10..A16` | 离线 harness 继续从同一条用户报错链推进，依次暴露 `虫毒 -> bf.Log self`、`蛇毒 -> function value`、`蛇毒 -> bf.Log self`、`蟾毒 -> short bf.Log self`、`蟾毒 -> long bf.Log self` | `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_buff_maxchains_fix15 -WslDistro Ubuntu-24.04` 通过，`summary.json` 中 `buff.lua` 为 `onroundbuff_fail=0`；转换日志新增精确命中 `proto=0 pc=954/1055 apply proto0 BUFF_OnRoundBuff attribute poison prechain fix...`、`pc=973 apply ... attribute late-poison producer fix...`、`pc=1026 apply ... insect bf.Log self fix...`、`pc=1130 apply ... toad short bf.Log self fix...`、`pc=1157 apply ... toad long bf.Log self fix...`；需重编并同步 `so`，so 不提交 |
| 2026-05-05 | `待提交` | 修复 `AttackLogic_extendTalents2` 的 `"吸星大法改"` callback caller：在 [tolua_fr1_to_fr2.c](E:/Games/work/tolua_runtime_master/tolua_fr1_to_fr2.c) 只按 `proto320` 一组详细指令形态补丁 `MOV A15<-A14; MOV A16<-A0; MOV A17<-A1; MOV A18<-A2; MOV A19<-A3; MOV A20<-A4; MOV A21<-A5; MOV A22<-A9; CALL A15 C8`，右移为 GC64/FR2 的 `MOV A17..A23; CALL A15 C8`，保留 `A16` 空槽。同步补强离线工具：`run_arm64_offline_regression.ps1` 的 `proto320 callback call` 静态门禁新增第二个精确窗口；`bcrun_cli.c` 新增 `attacklogic_extendtalents2_xixingcallback` 诊断 harness，但明确不纳入 gating | 用户新报错：`AttackLogic_extendTalents2 -> tmp.lua:5703 attempt to compare nil with number`，栈回到 `<tmp.lua:5702>` 和 `tmp.lua:6645` | 根因不是子 proto `5702/5703` 本身，而是 `proto320 line6645` 的 `CALL(C=8)` 仍停在 FR1 连续参数窗，GC64 参考要求 `A17..A23`；补丁后 `tmp/offline_regression_proto320_xixing_callback_final/attacklogic.proto320.extendtalents2_callbackcall.dis.txt` 已对齐为 `line6644 MOV A17..A22`、`line6645 MOV A23; CALL A15 C8`。单文件 `xixing` harness 额外暴露出工具边界：当前 stub 在 `tmp.lua:26986` 顶层副作用后无法继续注册该 workflow，所以只保留为诊断，不作为回归门禁；可信门禁仍是 `proto320` 静态窗口加现有 fullprobe/misslog。完整回归 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto320_xixing_callback_final -WslDistro Ubuntu-24.04` 通过，`summary.json` 中 `AttackLogic.lua` 为 `extendtalents_shape_fail=0`、`extendtalents2_log_fail=0`；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260505-proto320-xixing-callback-c8`，两个插件目录 `libtolua.so` SHA256 一致为 `9F2A1106E2197A05125162D715DEC44ECEDAEF8C35E7DD8124E3C986EC7F4F57`；so 不提交 |
| 2026-05-05 | `待提交` | 检查“FR1->FR2 代码拆分到 `tolua_fr1_to_fr2.c`”是否引入回归，并补齐 `battle.lua` 的 `proto132` 一组 `HasBuff` method 调用窗口：仅按详细指令形态 `ISNEXT A13; MOV A16<-A1; TGETS A15 B=1 C=49 D=305; MOV A17<-A14; CALL A15 B2 C3` 修为 GC64/FR2 的 `MOV A17<-A1; MOV A18<-A14`，保留 `A16` 为空槽；同步把 `proto132 pc1143..1149` 加入 `tools/run_arm64_offline_regression.ps1` 静态门禁 | 用户在拆分后报错：`BATTLE_BeforeRoleAction -> tmp.lua:3031 bad argument #1 to HasBuff (JyGame.BattleSprite expected, got string)`，并要求先检查是否是拆分错漏 | 先用同一输入对比拆分前 `289323e:tolua.c` 和当前 `tolua_fr1_to_fr2.c` 的 `battle.lua` 转换产物，两个文件 SHA256 完全一致，证明拆分本身没有改坏转换结果；真正根因是这组 `HasBuff` method call 在拆分前后都一直未修，当前产物仍是 `A16/A17`，而 GC64 参考要求 `A17/A18`。补丁后离线全回归需重新执行并重编插件；新版 build tag 更新为 `arm64fr2-20260505-proto132-hasbuff-c3`，so 不提交 |
| 2026-05-05 | `待提交` | 将 FR1->FR2 字节码转换实现从 [tolua.c](E:/Games/work/tolua_runtime_master/tolua.c) 提取到独立编译单元 `tolua_fr1_to_fr2.c`，保留 `tolua_loadbuffer` 和 `tolua_bytecode_android_ctor` 在 `tolua.c`；`tolua.h` 新增 `tolua_isbytecode` 导出；`android/jni/Android.mk`、`tools/run_arm64_offline_regression.ps1`、`tools/run_installyinjian_regression.ps1` 的构建入口改为显式编译 `tolua_fr1_to_fr2.c`；`tools/check_arm64_fr2_log.ps1` 增加对新文件的跟踪 | 需求变更：希望把所有 FR1 转 FR2 代码抽成独立文件，便于后续移植到 `E:\Games\work\ulua_runtime` | 这次是结构重构，不改转换规则。离线全回归 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_extract_fr1_to_fr2 -WslDistro Ubuntu-24.04` 通过，`summary.json` 中 `battle.lua`/`AttackLogic.lua` 全部门禁保持 0；Android `arm64` 构建通过，日志确认 `Android.mk` 已编译 `tolua.c` 和 `tolua_fr1_to_fr2.c` 两个单元；新版插件 build tag 更新为 `arm64fr2-20260505-extract-fr1-fr2-core`，两个插件目录 `libtolua.so` SHA256 一致为 `C6714E353170BED0F4731AE8871B3D626160E66FC9748EE8E0B2C5BAB5D25D78`；so 不提交 |
| 2026-05-05 | `待提交` | 修复 `battle.lua` 的 `BATTLE_BeforeRoleAction` 直调 `math.min` 参数窗，只按 `proto132` 一组详细指令形态补丁：`GGET A6; TGETS A6 D=1603; SUBNV A7 B=5 D=1283; TGETS A8 B=4 D=1092; TGETS A8 B=8 D=2117; IST C=8; JMP; KSHORT A8 D=0; CALL A6 B2 C3`，右移为 GC64/FR2 的 `A8/A9` 参数链；同步补强离线工具：`tools/bcrun_cli.c` 新增 `battle_beforeroleaction_mincall` harness，直接执行 `BATTLE_BeforeRoleAction` 并在过完该 `min` 后用 `prevRole.workflows.BeforeRoleAction` 哨兵中断；`tools/run_arm64_offline_regression.ps1` 新增 `proto132 pc297..305` 静态门禁和该 harness 的 gating | 用户新报错：`BATTLE_BeforeRoleAction -> tmp.lua:2811 bad argument #2 to min (number expected, got string)` | 根因不是 `BeforeRoleAction_forTalent` 注册链，而是 `BATTLE_BeforeRoleAction` 本体里的两参 direct `CALL(C=3)` 仍停在 FR1 槽位。新 harness 在修前直接复现：`BATTLE_BeforeRoleAction err=__BRA_MIN_BAD__ a=number:5 b=boolean:true`；补丁后 `tmp/offline_regression_proto132_beforeroleaction_minc3/battle.proto132.beforeroleaction_mincall.dis.txt` 已对齐为 `pc299 SUBNV A8`、`pc300/301 TGETS A9`、`pc302 IST C=9`、`pc304 KSHORT A9`、`pc305 CALL A6 C3`，`battle.beforeroleaction_mincall.log` 为 `run ok`；完整回归 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto132_beforeroleaction_minc3 -WslDistro Ubuntu-24.04 -SkipBuild` 通过，`summary.json` 中 `battle.lua` 新增 `beforeroleaction_fail=0` 且现有 battle/AttackLogic 门禁保持 0；本次已改 `tolua.c`，需重编并同步 `so`，so 不提交 |
| 2026-05-05 | `待提交` | 修复 `AttackLogic_RoleValues` 的 `RoleValuesDef` callback 参数窗：只按 `proto365` 一组详细指令形态补丁 `MOV A16<-A15; MOV A17<-A5; MOV A18<-A6; MOV A19<-A2; MOV A20<-A7; MOV A21<-A3; MOV A22<-A4; CALL A16 C7`，右移为 GC64/FR2 的 `A18..A23`，保留 `A17` 空槽，避免第 6 个参数落成 `nil`。同步补强离线工具：`tools/run_arm64_offline_regression.ps1` 新增 `proto365 line7608` 静态门禁；`tools/bcrun_cli.c` 的 `attacklogic_tempvalue_chain` 现在会给攻击方加 `失准` buff，并第二次执行 `AttackLogic_RoleValues`，真实走到 `RoleValuesDef` callback 的 `_talentMiss` 比较分支 | 用户新报错：`AttackLogic_RoleValues -> tmp.lua:7778 attempt to compare number with nil`，栈回到 `tmp.lua:7608` 和子 proto `<tmp.lua:7767>` | 根因不是 `line7778` 本身的比较指令，而是 `proto365 line7608` 这组 `CALL(C=7)` 只传到 5 个有效参数，`prevRoleDef` 落成了 `nil`，子 proto `proto369 lines=7767-7792` 在读 `prevRoleDef._talentMiss` 时触发 `0 >= nil`。补丁后 `tmp/offline_regression_rolevalues_defcallback_fix4/attacklogic.proto365.rolevalues_defcallback.dis.txt` 已对齐为 `line7606/7607/7608 MOV A18/A19/A20/A21/A22/A23; CALL A16 C7`；强化后的 `attacklogic.tempvalue_chain.log` 为 `run ok`；完整回归 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_rolevalues_defcallback_fix4 -WslDistro Ubuntu-24.04` 通过，`summary.json` 中 `AttackLogic.lua` 保持 `tempvalue_shape_fail=0`、`extend3_shape_fail=0`、`rolevalues_shape_fail=0`、`rolevalues_log_fail=0`、`extendtalents_shape_fail=0`、`tempvalue_chain_fail=0`、`extendtalents2_log_fail=0`；本次已改 `tolua.c`，需重编并同步 `so`，so 不提交 |
| 2026-05-04 | `待提交` | 修复 `GameEngine_extendStoryAction` 回归：收窄 `CALL(C=3)` 的 direct passthrough fast-path，排除 `MOV self<-obj; TGET* func<-obj[method]; MOV arg1<-param; CALL` 这类 method-style self forwarding，避免 `this:ExecuteNextStoryAction(callback)` 被误判成“已是 FR2 正确布局”而保留 FR1 槽位。同步修正 `tools/run_arm64_offline_regression.ps1` 的 `GameEngine proto108` 静态门禁，改为按反汇编行结构解析，不再使用错误的换行正则 | 用户新报错：`GameEngine_extendStoryAction -> tmp.lua:2725 bad argument #2 to ExecuteNextStoryAction (JyGame.CommonSettings.VoidCallBack expected, got number)` | 根因不是 `callback` 局部变量被覆盖，而是 `proto108` 七个 `ExecuteNextStoryAction(callback)` method 调用窗口都被快路径误跳过，`self/arg1` 停在 `A8/A9`，FR2 下应为 `A9/A10`。补丁后 `tmp/offline_regression_gameengine_storycallback_fix2/gameengine.proto108.executenextstoryaction.dis.txt` 中 `line2649/2654/2677/2715/2725/2745/2750` 全部对齐为 `MOV A9; TGETS A7; MOV A10; CALL A7 C3`；完整回归 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_gameengine_storycallback_fix2 -WslDistro Ubuntu-24.04` 通过；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-proto108-storyaction-callbackc3`，两个插件目录 `libtolua.so` SHA256 一致为 `21CED3BECA26D5FA770846586F05E803A8EF43867C823694C8F4712A2A6B489A`；so 不提交 |
| 2026-05-04 | `待提交` | 修复 `proto364` 中一组残留的 `CALL(C=10)` 参数窗口，只按详细指令形态补丁：`MOV A20<-A19; MOV A21<-A0; MOV A22<-A1; MOV A23<-A2; MOV A24<-A3; MOV A25<-A4; MOV A26<-A5; MOV A27<-A14; MOV A28<-A6; MOV A29<-A7; CALL A20 C10` 修为 GC64/FR2 的 `MOV A22..A30; CALL A20 C10`，保留 `A21` 空槽，避免内部子 proto 收到左移后的实参。同步补强离线工具：`tools/run_arm64_offline_regression.ps1` 新增 `proto364 callback C10` 静态门禁；`tools/bcrun_cli.c` 只修正旧 combo stub 的两个假错根因：`LuaTool.CreateLuaTable(nil)` 视为空表、`role.EquippedTitle=false`，但不把该 combo harness 重新纳入 gating | 用户新报错：`AttackLogic_extendTalents3 -> tmp.lua:7231 attempt to index local 'prevRoleDef' (a nil value)`，栈回到 `tmp.lua:7542` | 根因不是 `GetRrevRole` 或 workflow 注册，而是 `proto364 pc=596` 这组 `CALL(C=10)` 仍保持 FR1 连续参数窗，导致内部子 proto `<tmp.lua:7229>` 的 `prevRoleDef` 实参左移丢失。补丁后 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto364_prevroledef_call_v5 -WslDistro Ubuntu-24.04` 通过；`summary.json` 中 `AttackLogic.lua` 为 `extend3_shape_fail=0`、`extendtalents_shape_fail=0`、`extendtalents2_log_fail=0`、`tempvalue_fail=0`、`tempvalue_chain_fail=0`；转换日志命中 `proto=364 pc=596 apply proto364 callback call C10 arg fix A21..A29 -> A22..A30`；关键反汇编已对齐到 `line=7542 MOV A22..A30; CALL A20 C10`；本次已改 `tolua.c`，需重编并同步 `so`，so 不提交 |
| 2026-05-04 | `待提交` | 修复 `AttackLogic_extendTalents2` 的 `tmp.lua:6346` 早期 miss 分支 `bf:Log` self 错位，只按 `proto320 pc=94..100` 的详细指令形态补丁：`MOV A11<-A3; TGETS A10 B=3 C=26; TGETS A12 B=1 C=27; TGETS A12 B=12 C=4; KSTR A13 D=28; CAT A12..A13; CALL A10 C3` 修为 GC64 的 `MOV A12<-A3; TGETS A10 B=3 C=26; TGETS A13 B=1 C=27; TGETS A13 B=13 C=4; KSTR A14 D=28; CAT A13..A14; CALL A10 C3`。同步补强离线工具：`tools/run_arm64_offline_regression.ps1` 新增 `proto320 line6346` 静态门禁；`tools/bcrun_cli.c` 的 `attacklogic_extendtalents2_misslog` 改为复用 `attacklogic_extendtalents2_fullprobe` 的单文件 stub，只额外强制 `result.Hp=0` 并在 `bf:Log("闪避了本次攻击！")` 处打哨兵，避免再次回到 `RegisterPrevRole/CreateLuaTable` 旧漂移点 | 用户新报错：`AttackLogic_extendTalents2 -> tmp.lua:6346 bad argument #1 to Log (JyGame.BattleField expected, got string)` | 旧 misslog combo harness 先暴露出测试工具误用：静态 bad-pattern 写宽、battle+AttackLogic 双文件 stub 先卡在 `CreateLuaTable arg=nil`，并不能证明 `line6346`。收紧到 `line=6346` 本身并切到 fullprobe 同源单文件 harness 后，`powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto320_misslog_fix2 -WslDistro Ubuntu-24.04` 通过；`summary.json` 中 `AttackLogic.lua` 为 `extendtalents_shape_fail=0`、`extendtalents2_log_fail=0`、`tempvalue_chain_fail=0`；`attacklogic.extendtalents2.misslog.log` 为 `run ok`；转换日志命中 `proto=320 pc=100 apply proto320 miss bf.Log self fix A11..A13 -> A12..A14`；本次已改 `tolua.c`，需重编并同步 `so`，so 不提交 |
| 2026-05-04 | `待提交` | 修复 `battle.lua` 的 `BATTLE_BeforeSkillAnimation` callback 调用错位，只按 `proto177` 两组 `CALL(C=6,B=1)` 详细窗口补丁：1) `MOV A11<-A10; MOV A12<-A0; MOV A13<-A1; MOV A14<-A2; MOV A15<-A3; MOV A16<-A5; CALL A11 C6` 修为 GC64 的 `MOV A11<-A10; MOV A13<-A0; MOV A14<-A1; MOV A15<-A2; MOV A16<-A3; MOV A17<-A5; CALL A11 C6`；2) `TGETS A12 B=11 C=119; MOV A13<-A0; MOV A14<-A1; MOV A15<-A2; MOV A16<-A3; MOV A17<-A5; CALL A12 C6` 修为 `MOV A14..A18`。同步补强离线工具：`tools/bcrun_cli.c` 新增 `battle_beforeskillanimation_callback` harness，直接验证 `BATTLE_BeforeSkillAnimation` 调 callback 时 5 个实参顺序；`tools/run_arm64_offline_regression.ps1` 新增 `proto177` 静态门禁和该 harness 的 gating。另修正 WSL 离线工具链：先重建 `luajit-2.1/src` 的 `x86_64 GC64` 主机版 `libluajit.a`/`luajit`，避免之前被 Android 交叉编译残留成 `AArch64` 导致 `bcrun_cli_wsl` 假通过 | 用户新报错：`BATTLE_BeforeSkillAnimation -> tmp.lua:3458 attempt to index field 'Role' (a nil value)`，栈回到 `tmp.lua:4158` | 先用新 harness 在旧产物上直接复现根因：callback 实参实际变成 `sprite, skill, 1, prevRole, function`，证明 `battlefield` 被吃掉、整条 `CALL(C=6)` 参数块左移 1 格；对照真正可执行的 `battle.source.gc64.bin` 后确认当前 `battle.gc64.fr2.bin` 不是可靠参考，真实 GC64 `proto177` 在 `pc744..750 / pc772..778` 必须保留 `A12/A13` 空槽。补丁后 `battle_beforeskillanimation_callback` harness 对当前转换产物通过，`tmp/battle.proto177.curr.dis.txt` 已对齐到 `MOV A13..A17` 和 `MOV A14..A18`，转换日志命中 `proto=177 pc=760/788 apply proto177 BeforeSkillAnimation callback ...`。完整回归 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto177_beforeskillanimation -WslDistro Ubuntu-24.04` 通过，`summary.json` 中 `battle.lua` 新增 `beforeskillanimation_fail=0` 且 battle/AttackLogic 现有门禁保持 0；本次已改 `tolua.c`，需重编并同步 `so`，so 不提交 |
| 2026-05-04 | `待提交` | 补齐 `AttackLogic_extendTalents2` 在 `proto320` 的 4 个残留 `bf.Log` 左偏窗口，只按详细指令形态修复：1) `line6384` `MOV A14; TGETS A13 C=26; TGETS A15 C=27; TGETS A15 C=4; KSTR A16 D=50; MOV A17<-A12; KSTR A18 D=51; CAT A15..A18; CALL A13 C3` 右移为 `A15/A16/.../A19`；2) `line6386` `MOV A14; TGETS A13 C=26; TGETS A15 C=27; TGETS A15 C=4; KSTR A16 D=52; CAT A15..A16; CALL A13 C3` 右移为 `A15/A16/A17`；3) `line6471/6473` `MOV A15; TGETS A14 C=26; KSTR A16 D=106; MULVN A17; KSTR A18 D=107; CAT A16..A18; CALL A14 C3` 右移为 `A16/A17/.../A19`；4) `line6481/6487` `MOV A14; TGETS A13 C=26; KSTR A15 D=110; TGETS A16 C=27; TGETS A16 C=4; KSTR A17 D=79; CAT A15..A17; CALL A13 C3` 右移为 `A15/A16/.../A18`。同步补强 `run_arm64_offline_regression.ps1` 的 `Test-AttackLogicExtendTalents2YihuaLogWindows`，把这 4 个窗口纳入静态 gate，避免只验 `6735/6744` 两个窗口 | 用户反馈“错误依旧”，上一轮离线 gate 通过但真机仍回到 `AttackLogic_extendTalents2/3 -> bf:Log self got string`，根因是同一 `proto320` 里还有 4 个未覆盖的旧窗口 | `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto320_logwindows_complete -WslDistro Ubuntu-24.04` 通过，`summary.json` 中 `AttackLogic.lua` 保持 `extendtalents2_log_fail=0`、`tempvalue_chain_fail=0`、`extendtalents_shape_fail=0`；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-proto320-logwindows-complete`，两个插件目录 `libtolua.so` SHA256 一致为 `78ECEAF780E975C592182AB27F733F1DA73083D65AB3E86218C5449048AF7BAC`；so 不提交 |
| 2026-05-04 | `待提交` | 继续收窄 `AttackLogic_extendTalents2` 的 `移花接玉` 反弹负面状态链，只按两组 `proto320` 详细指令形态修 `bf:Log` 的 self/message 左偏：1) `MOV A14<-A3; TGETS A13 C=26; TGETS A15 C=27; TGETS A15 C=4; KSTR A16 D=180; MULVN A17; KSTR A18 D=181; MOV A19<-A11; CAT A15..A19; CALL A13 C3` 右移为 native/FR2 的 `A15/A16/.../A20`；2) `MOV A14<-A3; TGETS A13 C=26; TGETS A15 C=27; TGETS A15 C=4; KSTR A16 D=183; MOV A17<-A11; KSTR A18 D=172; CAT A15..A18; CALL A13 C3` 右移为 `A15/A16/.../A19`。同步补强离线工具：`run_arm64_offline_regression.ps1` 新增 `proto320 yihua bf.Log` 静态门禁；`bcrun_cli.c` 新增 `attacklogic_extendtalents2_yihualog` 诊断 harness，但明确不纳入 gating，因为离线单文件加载 `AttackLogic.lua` 会先在 `tmp.lua:26986` 顶层中断，导致 `line6735` 所在的后段 workflow 未注册进 `BattleUtil.GetWorkflowForTalent` | 用户新报错：`AttackLogic_extendTalents2 -> tmp.lua:6735 bad argument #1 to 'Log' (JyGame.BattleField expected, got string)` | 反汇编已确认当前错误就是 `proto320 pc=1572/1597` 两个相邻 `bf.Log` 窗口残留 FR1 槽位：修前分别是 `MOV A14 / TGETS A15 / TGETS A15 / ... / CAT A15 / CALL A13`，修后与 GC64 参考对齐为 `MOV A15 / TGETS A16 / TGETS A16 / ... / CAT A16 / CALL A13`；`powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto320_yihua_bflog_v3 -WslDistro Ubuntu-24.04` 通过，`summary.json` 中 `AttackLogic.lua` 为 `extendtalents_shape_fail=0`、`tempvalue_fail=0`、`tempvalue_chain_fail=0`、`extendtalents2_log_fail=0`；`attacklogic.extendtalents2.fullprobe.log` 为 `run ok`；本次已改 `tolua.c`，需重编并同步 `so`，so 不提交 |
| 2026-05-04 | `待提交` | 继续收窄 `AttackLogic_extendTalents2`，先把离线 gate 收敛到真正有效的三层：`attacklogic_tempvalue` 根注册 harness、`proto241/316/317/318/319/320/364/389` 静态门禁、`attacklogic_extendtalents2_fullprobe` 全链路 harness；移除 `tempvalue_chain` 与 `gedanglog/chaizhaolog` 旧 battle+AttackLogic 组合 harness 的门禁地位，原因是它们已经被 fullprobe 覆盖，却持续因 stub 漂移产生假失败。然后在 `tolua.c` 只按两组 `proto320` 详细指令形态修 `xiValue` 链中的两参 `math.min` 参数槽：1) `ADDVN A13 B=12 C=26; GGET A14 D=44; TGETS A14 B=14 C=123; KSHORT A15 D=30; TGETS A16 B=0 C=27; TGETS A16 B=16 C=96; CALL A14 B2 C3` 右移为 native/FR2 的 `A16/A17`；2) `ADDVV A12 B=13 C=14; GGET A13 D=44; TGETS A13 B=13 C=123; KNUM A14 D=18; MOV A15<-A12; CALL A13 B2 C3` 右移为 `A15/A16` | 用户给出 `AttackLogic_extendTalents2` 仅剩 native 地址栈，之前离线 fullprobe 先后暴露并清掉了 `proto320` callback caller、`proto2` 假缺字段、`prevRole` 初始化缺失，最终推进到真实下一条：`tmp.lua:6728 bad argument #2 to min (number expected, got nil)` | 反汇编已确认这不是 broad 回归，而是 `proto320` 两组相邻 `math.min` 仍停在 FR1 槽位：当前产物 `line6727/6728` 为 `KSHORT A15; TGETS A16; TGETS A16; CALL A14` 和 `KNUM A14; MOV A15; CALL A13`，GC64 参考 `line7711/7714` 分别要求 `A16/A17` 与 `A15/A16`。补丁后 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto320_xivalue_fix3 -WslDistro Ubuntu-24.04 -SkipBuild` 通过，`summary.json` 中 `AttackLogic.lua` 为 `extendtalents_shape_fail=0`、`tempvalue_fail=0`、`tempvalue_chain_fail=0`、`extendtalents2_log_fail=0`；`attacklogic.extendtalents2.fullprobe.log` 为 `run ok`；本次已改 `tolua.c`，需重编并同步 `so`，so 不提交 |
| 2026-05-04 | `待提交` | 修正 `AttackLogic_extendTalents2` 的真实 `proto364` 根因，并同步收紧离线门禁到 GC64 参考形态：先用 `attacklogic_source_gc64_dbg.fr2.bin` 对照确认旧门禁虽然已从 `proto320` 改到 `proto364`，但仍把“当前错误产物”当成正确形态；随后在 `tolua.c` 只按两组详细指令窗口修复 `proto364` 的 `bf.Log` self/message 左偏 1 格问题：1) `MOV A14; TGETS A13 C=30; KSTR A15 D=31; MOV A18; TGETS A16 C=28; KSTR A19 D=26; CALL A16 C3; TGETS A16 C=29; MULNV A16; KSTR A17 D=32; CAT A15..A17; CALL A13 C3` 右移为 `A15/A16/.../A20`；2) `MOV A14; TGETS A13 C=30; KSTR A15 D=52; GGET A16; TGETS A16 C=23; MOV A18; CALL A16 C2; KSTR A17 D=53; CAT A15..A17; CALL A13 C3` 右移为 `A15/A16/.../A19`；`tools/run_arm64_offline_regression.ps1` 的 `proto364` 两组静态门禁同步改为匹配 GC64 参考窗口，不再接受旧的左偏形态 | 用户持续报 `AttackLogic_extendTalents3 -> tmp.lua:7483 bad argument #1 to 'Log' (JyGame.BattleField expected, got string)`，且已确认运行时输入就是 `lua/luajit` 解码结果、`so` 文件本身无异常 | 参考 GC64 `proto364` 反汇编已确认旧产物确实左偏：`line7431/7483` 原先是 `MOV A14 / KSTR A15 / GGET A16 ... / CAT A15`，修后与 GC64 `line8468/8525` 对齐为 `MOV A15 / KSTR A16 / GGET A17 ... / CAT A16`；`powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto364_bflog_fix -WslDistro Ubuntu-24.04` 通过，`summary.json` 中 `AttackLogic.lua` 保持 `extendtalents2_log_fail=0`、`extendtalents_shape_fail=0`、`tempvalue_chain_fail=0`；本次已改 `tolua.c`，需重编并同步 `so`，so 不提交 |
| 2026-05-04 | `待提交` | 修正 `AttackLogic_extendTalents2` 离线回归门禁误指向：先用 `bc_dump_proto` 摘要确认真实目标不是旧脚本里硬编码的 `proto320`，而是 `proto364 lines=7370-7550`；随后将 `tools/run_arm64_offline_regression.ps1` 中 `extendTalents2 gedang/chaizhao` 两组静态门禁、反汇编产物名和失败提示全部切到 `proto364`，并按当前转换产物的两个真实 `bf.Log` 指令窗口更新正则，消除“工具通过但实际没检查到报错点”的假阳性 | 用户确认 `lua/luajit` 就是 `lua` 目录现场解码结果，`so` 文件本身无异常；继续追 `AttackLogic_extendTalents3 -> tmp.lua:7483 bad argument #1 to Log` 时，发现旧离线脚本仍在检查 `proto320` | 首次用 `proto364` 跑回归即暴露工具误指向：`offline_regression_proto364_fix` 报 `missing proto364 extendtalents2 ... bf.Log window`；修正正则后，`powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_proto364_fix2 -WslDistro Ubuntu-24.04 -SkipBuild` 通过，`summary.json` 中 `AttackLogic.lua` 保持 `extendtalents_shape_fail=0`、`extendtalents2_log_fail=0`；本次未改 `tolua.c`，未重编/同步 `so` |
| 2026-05-04 | `待提交` | 检查并继续收窄 `AttackLogic_extendTalents2` 的 `拆招率` 链，确认不是上一轮 `格挡率 Log` 修复回归，而是同一 `proto320` 中更早两个相邻窗口未覆盖：1) `math.min` 详细形态 `GGET A13 D=44; TGETS A13 B=13 C=123 D=3451; ADDNV A14 B=10 C=3 D=2563; ADDNV A15 B=11 C=3 D=2819; DIVVV A14 B=14 C=15 D=3599; MULVN A14 B=14 C=16 D=3600; ADDVN A14 B=14 C=17 D=3601; KSHORT A15 D=1; CALL A13 B2 C3`，右移为 native/FR2 的 `A15/A16`；2) 紧随其后的 `bf:Log` 详细形态 `MOV A15<-A3; TGETS A14 B=3 C=26 D=794; TGETS A16 B=1 C=27 D=283; TGETS A16 B=16 C=4 D=4100; KSTR A17 D=130; GGET A18 D=44; TGETS A18 B=18 C=73 D=4681; SUBNV A20 B=13 C=1 D=3329; MULVN A20 B=20 C=3 D=5123; CALL A18 B2 C2; KSTR A19 D=75; CAT A16 B=16 C=19 D=4115; CALL A14 B1 C3`，右移为 `A16..A21`；同步新增 `attacklogic_extendtalents2_chaizhaolog` 专用 harness 和对应 `proto320` 静态门禁 | 用户反馈“刚才修改后错误条数变多”，并给出 `AttackLogic_extendTalents3 -> tmp.lua:7483 bad argument #1 to Log (JyGame.BattleField expected, got string)` | 反汇编已确认这不是上轮 `7590` 的回归，而是相邻未修窗口：当前转换产物 `proto320 line=6542/6543` 仍是 `MOV A15`, `TGETS A16`, `GGET/CALL A18`, `CAT A16`, 与 native `line=7491/7493` 的 `A15/A16` `math.min` 和 `A16..A21` `bf:Log` 不一致；补完后 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_extendtalents2_chaizhaochain_harness -WslDistro Ubuntu-24.04` 通过，`AttackLogic.lua` 行保持 `extendtalents_shape_fail=0`、`tempvalue_chain_fail=0` 且 `extendtalents2_log_fail=0`；专用 harness 日志为 `run ok: ... mode=attacklogic_extendtalents2_chaizhaolog`；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-extendtalents2-chaizhaochain-p320`，两个插件目录 `libtolua.so` SHA256 一致为 `82D1EBA226A9853DEF3913D870947407DC59E8419D0A8871A0BEC725C02314AB`；so 不提交 |
| 2026-05-04 | `待提交` | 继续修 `AttackLogic_extendTalents2` 的 `bf:Log(...)` self 错位，只命中 `proto320` 一组详细窗口：`MOV A17<-A3; TGETS A16 B=3 C=26 D=794; TGETS A18 B=1 C=27 D=283; TGETS A18 B=18 C=4 D=4612; KSTR A19 D=147; GGET A20 D=44; TGETS A20 B=20 C=73 D=5193; SUBNV A22 B=15 C=1 D=3841; MULVN A22 B=22 C=3 D=5635; CALL A20 B2 C2; KSTR A21 D=75; CAT A18 B=18 C=21 D=4629; CALL A16 B1 C3`，统一右移为 native/FR2 的 `A18..A23`；同时把 `attacklogic_tempvalue_chain` 恢复为只校验注册链，再新增 `attacklogic_extendtalents2_gedanglog` 专用 harness，用哨兵方式在“本次格挡率为”这条 `bf:Log` 处截停，避免后续无关逻辑污染判断 | 用户新报错：`AttackLogic_extendTalents2 -> tmp.lua:6613 bad argument #1 to 'Log' (JyGame.BattleField expected, got string)` | 旧组合 harness 已不再用于判断这条错误；新专用 harness 与 `proto320` 静态门禁一起通过：`powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_extendtalents2_gedanglog_harness -WslDistro Ubuntu-24.04` 返回 `[OK] Offline regression passed.`，`AttackLogic.lua` 行新增 `extendtalents2_log_fail=0` 且 `extendtalents_shape_fail=0`、`tempvalue_chain_fail=0`；专用 harness 日志为 `run ok: ... mode=attacklogic_extendtalents2_gedanglog`；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-extendtalents2-gedanglog-p320`，两个插件目录 `libtolua.so` SHA256 一致为 `ABE5322976DBDF7A99AF1F7CC1952D0326355A93D0362F1C450892ACF89A4230`；so 不提交 |
| 2026-05-04 | `待提交` | 继续修 `AttackLogic_extendTalents` 同一 `proto241` 内的两参 direct `CALL(C=3)` 参数错位，只命中这一组详细窗口：`GGET A15 D=21; TGETS A15 B=15 C=67 D=3907; TGETS A16 B=1 C=24 D=280; TGETS A16 B=16 C=31 D=4127; SUBVN A16 B=16 C=10 D=4106; KSHORT A17 D=1; CALL A15 B2 C3`，右移为 native/FR2 的 `A17/A18` 形态；同时把 `run_arm64_offline_regression.ps1` 的 `extendtalents_shape_fail` 门禁收紧为文本精确匹配，直接守 `HasBuff` 窗口和这组两参调用窗口 | 用户新报错：`AttackLogic_extendTalents -> tmp.lua:4049 calling 'HasBuff' on bad self (JyGame.BattleSprite expected, got number)`；修完 `HasBuff` 后，离线同链继续暴露 `tmp.lua:4259 bad argument #2 to 'x' (number expected, got string)` | 先用扩过的 `attacklogic_tempvalue_chain` harness 复现同一 proto 下一处错误：`tmp.lua:4259 bad argument #2 to 'x'`；对照 native GC64 `proto241` 后确认根因是 `math.max(targetSprite.Role.Level - 10, 1)` 的两参 direct call 仍停在 `A16/A17`，FR2 下应落在 `A17/A18`。修后 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_extendtalents_maxcall_p241_final -WslDistro Ubuntu-24.04 -SkipBuild` 通过，`AttackLogic.lua` 行 `extendtalents_shape_fail=0`、`tempvalue_chain_fail=0`；转换日志精确命中：`proto=241 pc=590 apply proto241 HasBuff self fix ...`、`proto=241 pc=1316 apply proto241 two-arg call fix A16/A17 -> A17/A18`；关键反汇编已对齐：`pc1312..1315` 变为 `TGETS A17 / TGETS A17 / SUBVN A17 / KSHORT A18`；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-extendtalents-maxcall-p241`，两个插件目录 `libtolua.so` SHA256 一致为 `F02A2433FCC7C99F00205D5571E2FBC545BAD9DE016A804AC933321AB36473B7`；so 不提交 |
| 2026-05-04 | `待提交` | 继续修 `AttackLogic_RoleValues` 内 `bf:Log(...)` 的 method/self 错位，只命中 `proto365` 一个详细窗口：`MOV A16<-A7; TGETS A15 B=7 C=42; TGETS A17 B=6 C=43; TGETS A17 B=17 C=9; KSTR A18 D=63; GGET A19 D=17; TGETS A19 B=19 C=64; SUBNV A21 B=14 D=3584; CALL A19 B2 C2; KSTR A20 D=65; CAT A17 B=17 C=20; CALL A15 B1 C3`，修为 native/FR2 的 `A17/A18/.../A22` 形态。同步补强离线工具：`attacklogic_tempvalue_chain` harness 的 `bf:Log`、`sprite:*`、`role:*` stub 增加 self 校验，避免再把 method/self 错位误判为通过；`run_arm64_offline_regression.ps1` 新增 `proto365 rolevalues bf.Log` 静态门禁 | 用户新报错：`AttackLogic_RoleValues -> tmp.lua:7693 bad argument #1 to 'qiwangShanbi' (JyGame.BattleField expected, got string)` | 修前 strict harness 直接复现：`bf.Log self=def-role本次闪避率为21%！`，证明 `bf:Log(...)` 的 self 被错传成拼接字符串；修后 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_rolevalues_bflog_a365 -WslDistro Ubuntu-24.04` 通过，`AttackLogic.lua` 行新增 `rolevalues_log_fail=0` 且 `tempvalue_chain_fail=0`；`attacklogic.proto365.rolevalueslog.dis.txt` 已对齐到 native：`MOV A17`, `TGETS A18`, `GGET/CALL A20`, `SUBNV A22`, `CAT A18...A21`；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-rolevalues-bflog-self-a365`，两个插件目录 `libtolua.so` SHA256 一致为 `C89F0FA9ECF757B76AD692DDF76376574202F6944A7466C46F03967BA20B72A7`；so 不提交 |
| 2026-05-04 | `待提交` | 继续修 `AttackLogic_RoleValues` 函数体内的 direct `math.min/max` 调用孔位，只命中 `proto365` 的 4 个详细窗口：1) `GGET A14; TGETS A14 D=3602; TGETS A15 B=5 D=1323; TGETS A15 B=15 D=3877; SUBVN A15; KSHORT A16 D=20; CALL A14 B2 C3`，右移到 `A16/A17`；2) `GGET A15; TGETS A15 D=3858; KSHORT A16 D=99; GGET A17; TGETS A17 D=4414; MOV A19<-A13; CALL A17 B2 C2; ADDNV A17; CALL A15 B2 C3`，右移到 `A17..A20`；3) `GGET A15; TGETS A15 D=3858; MOV A16<-A13; KSHORT A17 D=10; CALL A15 B2 C3`，右移到 `A17/A18`；4) `GGET A15; TGETS A15 D=3897; MOV A16<-A13; KSHORT A17 D=0; CALL A15 B2 C3`，右移到 `A17/A18`。同时把 `attacklogic_tempvalue_chain` harness 扩展为真实执行 `AttackLogic_RoleValues`，用 `crazy` 模式直接覆盖这条 `math.min` 链 | 用户新报错：`AttackLogic_RoleValues -> tmp.lua:7666 bad argument #2 to 'min' (number expected, got string)` | 修前新 harness 直接复现：`@bcrun_attacklogic_tempvalue_chain failed: tmp.lua:7666 bad argument #2 to 'min' (number expected, got nil)`；修后 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_rolevalues_mincall_a365_v2 -WslDistro Ubuntu-24.04` 通过，`AttackLogic.lua` 行 `tempvalue_chain_fail=0`、`rolevalues_shape_fail=0`；`proto365.afterpatch.dis.txt` 已对齐到 native：`line7666/7669/7680/7685` 四组参数载体均落在 FR2 目标槽位；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-rolevalues-mincall-a365`，两个插件目录 `libtolua.so` SHA256 一致为 `944C42E2307F0BB267A1F117A49E00A7E8948EDD6E050FDC98466EB60CE01278`；so 不提交 |
| 2026-05-04 | `待提交` | 修复 `AttackLogic_RoleValues` 的 workflow 根注册错位，并继续收窄为唯一错误指令形态：`proto389` 中仅命中 `GGET A36; TGETS A36 D=9265; KSTR A37 D=(815|818); KSTR A38 D=(816|819|821|824|569|683); KPRI A39; FNEW A40 D=(817|820|822|825|826|827); CALL A36 C5` 这 6 个窗口，统一右移为 `A38/A39/A40/A41`；同步补强离线工具，`bcrun_cli.c` 的组合 harness 追加 `extend_RoleValuesAtk/Def` 断言，`run_arm64_offline_regression.ps1` 新增 `rolevalues_shape_fail` 静态门禁 | 用户新报错：`AttackLogic_RoleValues -> tmp.lua:7591 bad argument #1 to '(for generator)' (table expected, got nil)` | 修前全量反汇编已定位到 6 个坏窗口：当前产物 `proto389 pc=3280/3287/3294/3306/3313/3320` 均保留 `KSTR A37 ...; KSTR A38 ...; KPRI A39; FNEW A40`；修后 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_rolevalues_a36c5 -WslDistro Ubuntu-24.04` 通过，`AttackLogic.lua` 行新增 `rolevalues_shape_fail=0` 且 `tempvalue_chain_fail=0`；`attacklogic.proto389.rolevalues.dis.txt` 已对齐为 `KSTR A38 / KSTR A39 / KPRI A40 / FNEW A41`；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-rolevalues-a36c5`，两个插件目录 `libtolua.so` SHA256 一致为 `3EFD87A191D13998576468630E774F869055CEBCF10086F7D9BDC72F6640B8DB`；so 不提交 |
| 2026-05-04 | `待提交` | 修复 `AttackLogic_extendTalents3` 的 root 注册错位根因，并收窄为唯一错误指令形态：删除前一轮误命中的 `proto389` 假窗口补丁，只保留 `GGET A36; TGETS A36 D=9265; KSTR A37 D=(731|779); KSTR A38; KPRI A39; FNEW A40; CALL A36 C5` 这一组，统一右移为 `A38/A39/A40/A41`；同步把 `run_arm64_offline_regression.ps1` 的 `extend3` 静态门禁改为全 `proto389` 形态扫描，禁止残留 `A37` 坏窗口 | 用户持续反馈插件已是最新，但仍报 `AttackLogic_extendTalents3 -> tmp.lua:7511 bad argument #1 to ipairs (table expected, got nil)` | 离线先用 `attacklogic_tempvalue_chain` 组合 harness 复现：`attacker3=nil`，并诊断出 `BattleUtil.PrevWorkflows` 被错误注册成 `奇招怪式_forTalent/心心相印_forTalent` 等键；修后 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_extend3_a36c5 -WslDistro Ubuntu-24.04` 通过，`AttackLogic.lua` 行 `extend3_shape_fail=0`、`tempvalue_chain_fail=0`；`attacklogic.tempvalue.chain.log` 返回 `run ok`；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-attacklogic-extend3-a36c5`，两个插件目录 `libtolua.so` SHA256 一致为 `CD2C9761BC32C931ACFFC4B1A385E743A104F536908C26F28B0846E064800C3B`；so 不提交 |
| 2026-05-04 | `待提交` | 继续修 `AttackLogic_extendTalents2` 的 `ipairs(nil)` 链，但先补离线工具根因定位能力：`tools/bcrun_cli.c` 新增同一 Lua state 顺序加载 `battle.lua + AttackLogic.lua` 的 `attacklogic_tempvalue_chain` harness，不再只验证 `PrevWorkflows` 根注册，而是直接验证 `RegisterPrevRole` 之后 `prevRole.workflows.extendTalents2_forAttackerTempValue / extendTalents2_forDefencerTempValue / extendTalents2_forAttacker / extendTalents2_forDefencer` 均为 table；`run_arm64_offline_regression.ps1` 将该组合 harness 纳入 `AttackLogic.lua` 门禁。定位后，在 `tolua.c` 仅对 `proto389` 中当前错误的 root `CALL(C=5)` 注册块追加后处理：详细形态为 `GGET A36; TGETS A36 D=9265; KSTR A37 D=657; KSTR A38 <talent>; KPRI A39; FNEW A40; CALL A36 C5`，统一右移为 `A38/A39/A40/A41` | 用户反馈“插件是新的”，并继续报 `AttackLogic_extendTalents3 -> tmp.lua:7511 bad argument #1 to ipairs (table expected, got nil)` | 旧组合 harness 直接复现：`prevRoleDef.workflows.extendTalents2_forDefencer = nil`，证明问题不在 tempvalue 消费点，而在更早的 root 注册窗口；修后 `./tools/bcrun_cli_wsl battle.chainprobe.fr2.bin attacklogic_tempvalue_chain attacklogic.chainprobe.fr2.bin` 返回 `run ok`。转换日志精确命中 `proto389 pc=2602/2609/.../2822/3347` 共 31 个窗口：`apply proto389 defencer root C5 fix ... flow=D657`；反汇编已对齐：例如 `pc2598..2601` 变为 `KSTR A38 D657 / KSTR A39 D658 / KPRI A40 / FNEW A41`。全量回归 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_attacklogic_defencer_fix -WslDistro Ubuntu-24.04 -SkipBuild` 通过，`AttackLogic.lua` 行新增 `tempvalue_chain_fail=0`；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-attacklogic-defencer-rootc5`，SHA256 `DAE7F521948464EC23BF3A7509FB022616144C99CCAE6246FB802E6317E71A21`；so 不提交 |
| 2026-05-04 | `待提交` | 修复 `AttackLogic` 的 tempvalue workflow 根注册错位：为离线工具补齐 `BattleUtil` 最小注册 stub，并新增 `attacklogic_tempvalue` harness，直接断言 `BattleUtil.PrevWorkflows['extendTalents2_forAttackerTempValue_forTalent']` / `['extendTalents2_forDefencerTempValue_forTalent']` 均存在且各自注册 2 个 callback；同时在 `run_arm64_offline_regression.ps1` 新增 `AttackLogic.lua` 专项门禁，静态校验 `proto389 pc=2864/2871/2878/2885` 四个根 `CALL(C=5)` 窗口必须为 `GGET/TGETS/KSTR A38/KSTR A39/KPRI A40/FNEW A41/CALL A36`，并将 `-SkipBuild` 下的 host tool readiness probe 收敛为 `test -x`，消除误重建。`tolua.c` 仅对这四个当前错误窗口加后处理：`KSTR/KPRI/FNEW A37..A40 -> A38..A41` | 用户新报错：`AttackLogic_extendTalents3 -> tmp.lua:7511 bad argument #1 to ipairs (table expected, got nil)` | 离线先用新 harness 复现：当前产物缺少 `extendTalents2_forAttackerTempValue_forTalent`，并错误生成 `孤独求败_forTalent`、`奇招怪式_forTalent`、`斗转星移_forTalent` 等键，定位到 `proto389` 根注册 `CALL(C=5)` 的首参仍落在 `A37`；修后 `powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_attacklogic_tempvalue_nowarn -WslDistro Ubuntu-24.04 -SkipBuild` 通过，新增 `AttackLogic.lua` 行 `converted_lines=1 conversion_failed=0`；`attacklogic.tempvalue.log` 为 `run ok`；关键反汇编已对齐：`proto389 pc=2860/2861/2862/2863` 等四组均为 `A38/A39/A40/A41`；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-attacklogic-tempvalue-rootc5`，SHA256 `DA120B27185C4C920686BEFAC28EB31981B6329DCC4F472BA14B16D5296D3960`；so 不提交 |
| 2026-05-04 | `待提交` | 新增 `battle_rest` 离线 harness，直接覆盖 `BATTLE_Rest -> RegisterPrevRole -> workflows.Rest -> string.format` 整条链；按错误点详细指令窗口修复 3 处：1) root `proto202` 的 `TGETS A29 / KSTR A30 / KSTR A31 / KPRI A32 / FNEW A33 / CALL A29 C5` 改为参数落在 `A31..A34`，恢复 `RegisterWorkflowForTalent("Rest", ...)` 注册；2) `proto19 line809` 的 `table.insert(prevRole.workflows[workflow.name], workflow.callbacks[tname])` 窗口从 `A20/A21` 右移到 `A21/A22`；3) `proto192 line4663` 的 `GGET A5 / TGETS A5 / KSTR A6 / TGETS A7 / TGETS A8 / CALLT A5 C4` 改为 `A7/A8/A9` | 用户新报错：`BATTLE_Rest -> tmp.lua:4658 bad argument #1 to ipairs (table expected, got nil)` | 离线先复现：`battle_rest` 先后报 `Rest_forTalent=nil`、`line809 table.insert arg1=function`、`ret=14`；逐条修后，`powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_battle_rest_fix4 -WslDistro Ubuntu-24.04 -SkipBuild` 通过，`battle.rest.log` 与 `battle.registerprevrole.log` 均为 `run ok`；关键反汇编已对齐：root `proto202` 注册窗口首参从 `A30` 修正到 `A31`，`proto19 line809` 为 `A21/A22`，`proto192 line4663` 为 `KSTR A7 / TGETS A8 / TGETS A9`；已重编并同步 Unity `arm64` 插件，build tag `arm64fr2-20260504-battle-rest-chain`，SHA256 `732334BE58C0B951FF1A8BA35C14C175A96A1E8D2BC5B57ACE87DF705286747B`；so 不提交 |
| 2026-05-04 | `待提交` | 仅按错误点详细指令窗口修复 `proto19` 三处 `CALL(B=1,C=3)`：`pc=722`（`UGET A14 / TGETS A14 / MOV A15 / TGETV A16 / CALL A14`）、`pc=90`（`UGET A16 / TGETS A16 / MOV A17 / TGETS A18[C=28] / TGETV A18[A15] / CALL A16`）、`pc=103`（同形态但 `TGETS C=31`）；同时收紧/修复离线工具：`run_arm64_offline_regression.ps1` 自动选择可编译 WSL 发行版，改为结构化校验 `proto19` 三个固定窗口，并以 `registerprevrole` harness 语义门禁覆盖两次连续触发调用 | 用户反馈 `BATTLE_BeforeInitBattle -> tmp.lua:254 attempt to index local 'trigger' (a function value)`，并要求后续修复只限定错误处指令形态 | 离线确认：`battle proto19` 三个目标窗口均已对齐到 FR2 目标槽位，其中 `pc=90/103` 均变为 `MOV A18<-A5; TGETS A19<-A11[...] ; TGETV A19<-A19[A15]; CALL A16 C3`；`powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1 -BytecodeDir E:\Games\work\jygame_renew\gamedata\modcache\SSWS_HG\lua\luajit -OutDir tmp\offline_regression_after_windows -WslDistro Ubuntu-24.04` 通过；`bcrun_cli_wsl battle.caller535.fix2.fr2.bin registerprevrole` 返回 `run ok`；已重编并同步 Unity arm64 插件，build tag `arm64fr2-20260504-proto19-directcall-windows`，SHA256 `2C277FB79A8B77C4CDBDF3A4645DC4E5699B216913A7AF1C520A15000AE89C1E`；so 不提交 |
| 2026-05-04 | `待提交` | 修复 `InstallYinjian` 中 `System.Type.GetType`/`MakeGenericMethod` 同链根因：`call-result+literal CALL(C=3)` 在 `proto10` 的 `CreateInstance` 链上保留了“外层函数槽已右移、内层 method self 仍停留在 A+1/A+3”的半对齐形态；现新增 `proto10` 独立后处理，补齐三组 `CreateInstance`（line180/187/194）与三组 `MakeGenericMethod(...):Invoke(...)`（line182/189/196）的 self/arg 槽位右移。同步扩展 `tools/run_installyinjian_regression.ps1`，将 `proto10 160..255` 纳入指纹门禁，覆盖这 6 段链路 | 用户新报错：`BATTLE_BeforeInitBattle -> tmp.lua:180 invalid arguments to method System.Type.GetType` | 离线反汇编已对齐：`line180` 变为 `MOV A19<-A5; TGETS A17; CALL A17 C2; KSHORT A18; CALL A15 C3`，后续 `line182/189/196` 的 `MakeGenericMethod/Invoke` 也全部右移到 `A+2..`；`run_installyinjian_regression -SkipBuild` 通过；已重编并同步 Unity arm64 插件，build tag `arm64fr2-20260504-installyinjian-gettype-chain`，SHA256 `EF6B4ED340A0DDC441F23D8A85F7DEA448A57F802CCA41860A72881A05E3D4BB`；so 不提交 |
| 2026-05-04 | `待提交` | 修复 `proto10 line147` 规则失效根因：原修复挂在 `line144` 严格分支内，当前输入常为“line144 已部分对齐”导致分支不命中；现将 `line146/line147` 的 `CreateInstance` 寄存器修复拆为独立 pass（不依赖 line144 命中）。同步完善 `tools/run_installyinjian_regression.ps1`：先做 `proto10` 指纹校验，再跑 harness；`incompatible bytecode` 仅记环境告警不再遮蔽规则结果 | 用户持续报 `BATTLE_BeforeInitBattle -> tmp.lua:147 invalid arguments to System.Array.CreateInstance` | 离线反汇编已对齐：`line146` 变为 `MOV A15<-A11`，`line147` 变为 `MOV A15<-A9; KSHORT A16; CALL A13 C3`；`run_installyinjian_regression -SkipBuild` 通过并输出兼容性告警；已重编并同步 Unity arm64 插件，SHA256 `D360E188E3155B9254A72E999B1D10E920F204DBFBBC7B97602E0D8C0C479DB2`；so 不提交 |
| 2026-05-04 | `待提交` | 完善离线测试工具链：`run_arm64_offline_regression.ps1` 固定先构建 GC64 LuaJIT，再编 `bcconv/bcrun/bcdump`；新增 `tools/run_installyinjian_regression.ps1`（GameEngine 专项：转换 + `installyinjian/checktrigger` harness + `proto10 line144/182` 指纹检查）；`bcrun_cli` 在 `incompatible bytecode` 时输出 GC64 环境提示 | 同一错误多次无法稳定复现/验证，工具链本身存在环境漂移 | 现在可一键复现 InstallYinjian 专项链路并在工具侧提前暴露环境不一致；后续每次修 `line144/line182` 必须先跑 `run_installyinjian_regression.ps1` 再给真机 |
| 2026-05-04 | `待提交` | 新增 `proto10 line144` 定向后处理：匹配 `CreateInstance` 前序四指令并重排为 `MOV A15 / TGETS A13 / CALL A13 / KSHORT A14`，确保随后的 `CALL A11 C3` 读取到 FR2 正确参数槽 | 用户持续反馈 `tmp.lua:144 invalid arguments to System.Array.CreateInstance` | 离线反汇编 `GameEngine.fix182k.fr2` 已与参考 FR2 在 `line144` 关键 5 条指令对齐；已重编并同步 Unity `arm64` so，SHA256 `01BB7A7A54FB578943C1407F1660E3A7A256D097933548258E141DD123DDE95D`；等待真机首条栈 |
| 2026-05-04 | `待提交` | 收窄 `call-result+literal CALL(C=3)` 规则：允许命中 `proto10 old_first=12`（恢复 `line144` 处单 `CALL` 形态），并保留 `old_first>=16` 场景以覆盖后段链路 | 用户新报错：`tmp.lua:144 invalid arguments to System.Array.CreateInstance` | 离线反汇编 `proto10 line144` 从错误形态（`MOV A14<-A13; MOV A13<-A12; CALL A11`）恢复为 `CALL A11 B2 C3`；已重编并同步 Unity `arm64` so，SHA256 `7B7CC140D6D36D606DAB3D8BBE060C82A90E59C1C97C6D8DF2C93516053D5EF5`；等待真机验证首条栈 |
| 2026-05-04 | `待提交` | 继续修 `InstallYinjian/MakeGenericMethod`：新增 `CALL(C=3)` 的“`CALL(C=2)` 结果 + 字面量尾参”窄跳过（`skip FR2 arg shift for call-result+literal`），避免误触发 `call-result aliases` 后插入双 `MOV`；并保留 `open CALL/CALLM` 链窗口为 `cpc-16` | 用户持续反馈 `tmp.lua:182 MakeGenericMethod` 参数类型错位 | 离线反汇编：`proto10 line182` 不再出现先前的“pre-shift 双 MOV 污染”（已从 `MOV A18<-A17; MOV A17<-A16; CALL A15` 纠正为 `TGETS A17...; MOV A19...; CALL A17` 方向）；已重编并同步 Unity `arm64` so，SHA256 `185B6C0BC09C37FC932D766859EB95026F84225AF19632219676864A905E3002`；等待你真机回传首条栈 |
| 2026-05-04 | `待提交` | 按要求仅保留 `tolua_bytecode_android_ctor` 的版本日志，并在该日志追加编译时间：`built_at=__DATE__ __TIME__` | 用户反馈“仍是同样错误”，要求版本日志包含构建时间以确认加载到最新插件 | Android ctor 日志现在会输出：`native build=... built_at=Mon DD YYYY HH:MM:SS loaded`；其余位置不新增版本日志；so 不提交 |
| 2026-05-04 | `待提交` | 修复 `InstallYinjian` 的 `MakeGenericMethod` 链寄存器错位：在既有 `open CALL(B=0,C>=4) -> CALLM(B=2,C=0) -> 一参 CALL` 后处理中，将局部回溯窗口从 `cpc-4` 扩到 `cpc-5`，让上游紧邻 `CALL(C=3)` 同步右移，避免结果仍停留在 base-1 槽位 | 用户新报错：`tmp.lua:182 bad argument #1 to MakeGenericMethod (MethodInfo expected, got RuntimeType[])` | 离线反汇编 `GameEngine.fix182.fr2` 关键点由 `CALL A16 B2 C3` 修正为 `CALL A17 B2 C3`（`proto10 pc=189`）；`battle` 离线 `getrrevrole` 通过；已重编并同步 Unity `arm64` 插件，SHA256 `2430036EFD3CBD851289505BFEFE895E135882316F95AD13F87B5150F7E7F555`；并确认旧 APK `金庸群侠传XR_nx111_202605031712.apk` 内置哈希仍为 `3DE0C6126AAD88431F9CDF0B8372C1A5BDF0C614280135B53082BFC8211DC4D5`（非最新） |
| 2026-05-04 | `待提交` | 版本日志可见性增强：`tolua_openlibs` 首次加载时强制打印一次 build（Lua `print` + native log 双通道），并在 Android 原生日志额外写入 `Unity` tag，避免只看 Lua 控制台时漏掉 `tolua_loadbuffer` 路径日志 | 用户反馈“前一次修改没有版本日志出现” | 新构建应在启动后出现一次：`[tolua-openlibs] build=arm64fr2-20260504-installyinjian-callm-chain ...`；已重编并同步 `arm64` so，最新哈希 `B8E0A8D38589BF971D7B496D0286AA4A2BF152A912C218F2B090D0375A9EDCA7`；so 不提交 |
| 2026-05-04 | `待提交` | 修复 `InstallYinjian` 链路中 `open CALL(B=0,C>=4) -> CALLM(B=2,C=0) -> 一参 CALL(C=2)` 的 FR2 基址错位：新增定向后处理，仅命中该链路时把前一条 open CALL 相关窗口右移一格（`base 15->16` 形态）；同时离线 `installyinjian` harness 保留 `CreateLuaTable` 参数类型硬校验 | 用户新报错：`tmp.lua:149 invalid arguments to method JyGame.LuaTool.CreateLuaTable`，栈 `InstallYinjian -> minValues -> CheckTriggerAllowed` | 离线日志命中：`proto=10 pc=87 apply open CALL/CALLM one-arg chain shift base 15->16 range=[15,19]->[16,20]`；反汇编与 GC64 原生 FR2 关键段对齐：`CALL A16 B0 C4; CALLM A14 B2 C0; MOV A17<-A14; CALL A15 B4 C2`；`checktrigger` 与 `battle/getrrevrole` 通过；so 不提交 |
| 2026-05-04 | `待提交` | 修复 `GameEngine proto10 line144` 的 `CALL` 结果寄存器回退：`simple-local` 新增保护，若最近 writer 是 `CALL/CALLM/...` 但未被选中则拒绝并强制走 `copy-fallback`；同时 `tools/bcrun_cli.c` 新增 `installyinjian` 离线模式，`tolua_loadbuffer` 首次调用固定打印构建信息（`build + __DATE__ __TIME__`） | 用户报错：`BATTLE_BeforeInitBattle ... tmp.lua:144 bad argument #1 to '?' (table expected, got userdata)`，并要求首次加载打印版本 | 新离线证据：`GameEngine.finalverify.fr2` 中 `proto10 line144` 从错误形态 `TGETS A13 + CALL A12` 修正为 `TGETS A12 + CALL A12`，并插入 `MOV` 搬移链；转换日志出现 `simple-local reject pc=69 reg=12 nearest=67(CALL) not-selected` + `copy-fallback ... old=[12,13] new=[13,14]`；`bcrun checktrigger`/`battle getrrevrole` 通过；重编 `arm64` 后两个插件目录 so 哈希一致 `E7BF9D75618F72AA664EFBA7AFFC812DAD15C2405CF4C9212DB36ED6183ADAF3`；so 不提交 |
| 2026-05-03 | `待提交` | 对 `method-self CALL(C=3)` 增加定点豁免：仅在 `proto=24 pc=201 base=10 old=[11,12]` 不走“skip FR2 arg shift”，避免把 `GameEngine/CheckTriggerAllowed` 的该调用误判为 method-self 透传；同时 `tools/bcrun_cli.c` 调整为“mode 下即使 chunk 顶层报错也继续执行 harness” | 用户持续反馈：`BATTLE_BeforeInitBattle -> CheckIfEquipNotRight -> CheckTriggerAllowed`，`tmp.lua:711 bad argument #1 to 'insert' (table expected, got string)`；离线日志显示该点曾命中 `skip FR2 arg shift for method-self CALL(C=3)` | `GameEngine` 转换日志由 `proto=24 pc=201 skip FR2 arg shift for method-self` 变为 `proto=24 pc=201 accept existing FR2 slice [12,13]`；battle 主门禁保持通过：`run_arm64_offline_regression` 通过，`check_fr1_callc2_tdup`（line 64/137/2765）通过，`check_fr2_call_frame_shift --require-line 601` 通过；so 不提交 |
| 2026-05-03 | `待提交` | 将 `copy-fallback`/`live-after` 的禁 spill 范围从 `CALL(B=1,C>=3)` 扩到所有 `CALL(C>=3)`；优先调用帧右移（`call-frame-shift`）或普通 copy-insert，避免 `CheckTriggerAllowed` 周边“spill+预移位”污染参数寄存器 | 用户新报错：`BATTLE_BeforeInitBattle -> CheckIfEquipNotRight -> CheckTriggerAllowed`，`tmp.lua:711 bad argument #1 to 'insert' (table expected, got string)` | 离线对照 `GameEngine.lua proto24`：旧规则 `proto=24 pc=103 live-after spill fallback spill=24 ... shift_count=2`；新规则为 `proto=24 pc=103 live-after fallback call-frame-shift copy insert base=12->13 args=[13,14]->[15,16]`，该点不再出现 spill；battle 关键门禁（line64/137/2765、line601、getrrevrole harness）继续通过；arm64 so 已重编并同步，build tag `arm64fr2-20260503-callc3-nosspill`，so 不提交 |
| 2026-05-03 | `待提交` | 删除 `battle proto8 pc3` 的 `LuaTool.CreateLuaTable CALL(C=2)` 特例；所有一参 `CALL(C=2)` 统一按 FR2 参数槽 `A+2`；修正 `tools/bcrun_cli.c` 的 `luanet.import_type` 缓存并新增 `getrrevrole` harness，直接离线触发 `RegisterPrevRole -> FilterTalents -> CreateLuaTable` | 用户反馈上一版错误依旧；根因是上一轮离线执行只跑顶层且 `import_type` 每次返回新 proxy，未覆盖 battle.lua 闭包里的真实 `LuaTool`。实际 GC64 源码参考显示 `FilterTalents` 的 `CreateLuaTable(csharpTalents)` 是 `MOV A5<-A0; CALL A3 C2`，不是 `A+1` | 旧规则离线 harness 复现 `tmp.lua:137 CreateLuaTable arg type=nil`；修后 `proto8 line137` 为 `MOV A5<-A0`，`tools/bcrun_cli_msvc.exe ... getrrevrole` 通过；`check_fr1_callc2_tdup.py --require-line 64/137/2765` 通过；`check_fr2_call_frame_shift.py --require-line 601` 通过；arm64 so 已用 NDK r21e 重编并同步到 Unity，build tag `arm64fr2-20260503-callc2-all-a2`，SHA256 `C8B05F75E4D1A98D142C2AA6FFF5E441CBED47981D8B7B73A5CDF78CA64D7A36`，so 不提交 |
| 2026-05-03 | `待提交` | 修正 `call-frame-shift` 分支的 FR2 调用帧布局：右移函数槽时，参数必须相对新函数槽放在 `A+2..`，不能继续放在 `A+1..`；新增 `tools/check_fr2_call_frame_shift.py` 离线门禁 | 真机回归为 `BATTLE_BeforeRoleAction tmp.lua:137 invalid arguments to method JyGame.LuaTool.CreateLuaTable`，栈显示经 `FilterTalents -> RegisterPrevRole -> GetRrevRole`；根因不是 line137 本身，而是 `RegisterPrevRole` 内 `FilterTalents(skill.Talents, tmap, otherTalents)` 的 `CALL(C=4)` 经整帧右移后仍按 FR1 参数槽调用，导致 `CreateLuaTable` 收到错误参数 | 离线确认：旧产物在 `proto19 pc324 line601` 失败（`CALL A15 C4` 参数落在 `A16..A18`）；修后为 `MOV A19<-A17; A18<-A16; A17<-A15; A15<-A14; CALL A15 C4`，参数落在 `A17..A19`；旧产物反测失败、新产物 `check_fr2_call_frame_shift --require-line 601` 通过；`battle` FR2 离线执行 `run ok`；arm64 so 已用 NDK r21e 重编并同步到 Unity，build tag `arm64fr2-20260503-callframe-fr2gap`，SHA256 `70336FC27D5153F08C51A8371E7902D25C992404A7000F3EB88FA7AF5A3D2AC4`，so 不提交 |
| 2026-05-03 | `待提交` | 将 `TGETS+MOV+CALL(C=2)` 的跳过规则进一步收窄为仅 `battle proto8 pc3` 的 `LuaTool.CreateLuaTable`；其它 Lua 表函数（如 `BattleUtil.GetRrevRole`）恢复 FR2 `CALL A+2` 参数槽 | 真机回归为 `tmp.lua:935 attempt to index local 'sprite' (a number value)`，调用栈来自 `tmp.lua:2765`；根因是上轮把所有 `TGETS+MOV+CALL(C=2)` 都保留在 `A+1`，导致 `BattleUtil.GetRrevRole(currentSprite)` 的 `sprite` 读到旧 number 槽 | 离线确认：`proto2 line64 ipairs` 为 `MOV A4<-A0`，`proto8 line137 CreateLuaTable` 保留 `MOV A4<-A0`，`proto132 line2765 GetRrevRole` 改为 `MOV A5<-A1`；`battle` FR2 离线执行 `run ok`；门禁反测旧 line64、line137、line2765 产物均失败；arm64 so 已用 NDK r21e 重编并同步到 Unity，build tag `arm64fr2-20260503-createluatable-only`，SHA256 `09BF217619B7B927220872AD239FF5FDED767A97F125C9DADDA11D7706B8A77A`，so 不提交 |
| 2026-05-03 | `待提交` | 将 mirrored `CALL(C=2)` 收窄为：`GGET/UGET + MOV` 继续按 FR2 右移到 `CALL A+2`，但 `TGET* + MOV` 表字段调用保留 `CALL A+1` | 上轮删除 mirrored 跳过过宽，真机回归为 `BATTLE_BeforeRoleAction tmp.lua:137 invalid arguments to method JyGame.LuaTool.CreateLuaTable`；`CreateLuaTable` 属于 `TGETS + MOV + CALL(C=2)`，不能和 `GGET ipairs` 一起右移 | 离线确认：修后 `proto2 line64` 为 `GGET ipairs; MOV A4<-A0; CALL A2 C2`，`proto8 line137` 为 `TGETS CreateLuaTable; MOV A4<-A0; CALL A3 C2`；`battle` FR2 离线执行 `run ok`；门禁反测旧 line64 产物失败、上轮过宽 line137 产物失败；arm64 so 已用 NDK r21e 重编并同步到 Unity，build tag `arm64fr2-20260503-callc2-mirror-narrow`，SHA256 `3B5F7FA1BE7C89850207B0841694B4F97154729AA7A37B19EDF871F3861E5350`，so 不提交 |
| 2026-05-03 | `待提交` | 删除 `GGET/TGET*/UGET + MOV + CALL(C=2)` mirrored 形态的跳过规则；该形态在 FR2 下必须把 `MOV` 实参从 `CALL A+1` 右移到 `CALL A+2` | 用户反馈 `List2Map/ipairs` 错误依旧，并要求检查离线测试工具；实际根因在 `List2Map` 子 proto：`ipairs(l)` 被错误保留为 `GGET A2; MOV A3<-A0; CALL A2 C2`，FR2 下 callee 读到旧槽 `10` | 新增/修正离线执行工具 `tools/bcrun_cli.c`：GC64 MSVC 可直接运行转换后的 FR2 bytecode 并带 traceback；旧产物离线复现 `tmp.lua:64 ipairs arg type=number value=10` / `tmp.lua:241`；修后 `proto2 line64` 为 `MOV A4<-A0`，`battle.fr2.bin` 离线执行 `run ok`；`main/battle/migong` 转换均通过；arm64 so 已用 NDK r21e 重编并同步到 Unity，build tag `arm64fr2-20260503-callc2-mirror-shift`，so 不提交 |
| 2026-05-03 | `待提交` | 修正 `tools/check_fr1_callc2_tdup.py` 默认检查语义：默认必须验证 FR2 转换产物；FR1 源文件需显式 `--expect-layout fr1`，并输出 `CALL A+1/A+2` 最近写入者 | 用户反馈错误依旧，要求检查离线测试工具；原工具在 FR1 源文件上也会输出 OK，容易把“源形态正确”误判为“FR2 转换正确” | 反测通过：旧错误产物 `battle.fr2.bin` 失败，显示 `slotA+2` 为 `KSHORT/TGETS/KSTR` 等非 TDUP；新产物 `battle_final.fr2.bin` 通过，显示 `slotA+2=TDUP`；直接检查 `lua/luajit/battle.lua` 默认失败并提示 version 1 |
| 2026-05-03 | `待提交` | 移除 `TGET*(tbl!=base)+TDUP+CALL(C=2)` 的跳过规则；该形态在 FR2 必须把 `TDUP` 从 `CALL A+1` 移到 `CALL A+2` | `jygame/battle.lua tmp.lua:64 bad argument #1 to ipairs`，`tmp.lua:169 List2Map` 仍复现；根因是上轮把 `TDUP A20 + CALL A19` 误判为正确 FR2 形态 | 离线解密确认 `lua/battle.lua` 解密结果与 `lua/luajit/battle.lua` SHA256 一致；转换后 `proto202 line169/241` 为 `TGETS A19; TDUP A21; CALL A19 C2`；`tools/check_fr1_callc2_tdup.py` 已改为校验 FR2 产物 `TDUP == CALL A+2`；MSVC 离线转换 `main/battle/migong` 均 `converted=1 failed=0`；arm64 so 已重编并同步到 Unity，build tag `arm64fr2-20260503-tdup-callc2-shift`，so 不提交 |
| 2026-05-03 | `481b37c` | 新增 `tools/check_fr1_callc2_tdup.py` 离线门禁，直接解析 SSWS_HG FR1 bytecode，检查 `TGET* + TDUP + CALL(C=2)` 形态；用 NDK r21e 本地重编/同步 arm64 `libtolua.so`，但 so 不纳入提交 | `jygame/battle.lua tmp.lua:64 bad argument #1 to ipairs`，`tmp.lua:169 List2Map` 首报错 | 结论已被本轮推翻：FR2 一参调用的正确实参槽是 `CALL A+2`，保留 `TDUP A20` 会让 `List2Map` 收到旧槽字符串 |
| 2026-03-27 | `待提交` | `CALL(C=2)` 改为“main+battle 混合窄规则”：保留 `e8660fb` passthrough 主路径；补回 `mirrored MOV` 与 `TGET*(tbl!=base)+TDUP` 两个定向跳过 | 同时满足 `main.lua line10 nil` 不回归 + `battle line137/169/241` 不错位 | 对照确认：`main.lua` 转换产物继续与 `e8660fb` MD5 一致（`d3405e575caac40eff41d2dbb6009c70`）；`battle proto8 line137` 恢复 `MOV A4<-A0`，`proto202 line169/241` 恢复 `TDUP A20`；离线回归通过 |
| 2026-03-27 | `待提交` | 回退 `CALL(C=2)` 到 `e8660fb` 的窄条件（仅 `MOV arg1<-passthrough + TGET*/UGET/GGET + CALL(C=2)` 跳过右移），移除 broad/镜像/`TDUP` 跳过 | `DoFile failed: jygame/main.lua err: tmp.lua:10: attempt to index a nil value` | 对照同源 `main.lua`：当前转换产物与 `e8660fb` MD5 完全一致（`d3405e575caac40eff41d2dbb6009c70`）；离线回归 `main/battle/migong` 全部 `conversion_failed=0` |
| 2026-03-27 | `待提交` | 将 `direct CALL(C=2)` 收敛为统一跳过 FR2 参数右移（覆盖 MOV/TDUP 等一参直调形态） | `tmp.lua:64 bad argument #1 to ipairs (table expected, got string)` 与 `line169 List2Map` 参数错位反复 | 离线回归 `offline_regression_semantic11` 通过；`proto202 line169/241` 均保持 `TDUP A20 + CALL A19 C2` |
| 2026-03-27 | `待提交` | `CALL(C=2)` 规则补充 `TDUP` 实参布局：支持 `func(TGET/UGET/GGET) + TDUP(arg) + CALL` 跳过右移，修复 `line169` 传入 `List2Map` 的参数错位 | `tmp.lua:64 bad argument #1 to ipairs (table expected, got string)` | 离线回归 `offline_regression_semantic10` 通过；`proto202 line169` 恢复 `TDUP A20`（不再被改为 `A21`） |
| 2026-03-27 | `待提交` | `CALL(C=2)` passthrough 规则补充镜像布局：支持 `func(TGET/UGET/GGET) + MOV(arg) + CALL`，避免 `line137 CreateLuaTable` 参数落到 `A+2` | `BATTLE_BeforeRoleAction tmp.lua:137 invalid arguments to CreateLuaTable` | 离线回归 `offline_regression_semantic9` 通过；`proto8 pc3` 命中 `skip FR2 arg shift for direct CALL(C=2)`，反汇编确认 `MOV A4<-A0` 恢复 |
| 2026-03-27 | `待提交` | 在 `tolua_shift_proto_slice_right_for_fr2` 的 live-after copy-insert 分支新增 `CALL(B=1,C>=4)` 整帧右移（含函数寄存器 `A`）并同步 `A+=1` | `BATTLE_BeforeInitBattle tmp.lua:711 bad argument #1 to insert (table expected, got string)` | 离线回归 `offline_regression_semantic8` 通过；`battle proto19 pc705/729` 由“仅参数右移”升级为“整帧右移 + `CALL A9->A10`”；关键指纹保持 `workflow_shift_hits=21` |
| 2026-03-27 | `待提交` | 在 `tolua_try_insert_copy_fallback_for_fr2` 的 live-after 分支补齐 `CALL(B=1,C>=4)` 保护：优先 copy-insert（禁用 spill 重写） | `BATTLE_BeforeInitBattle tmp.lua:711 bad argument #1 to insert (table expected, got string)` | 离线回归 `offline_regression_semantic6` 通过（`main/battle/migong conversion_failed=0`）；battle 日志仍保持 `proto19 pc704/727 live-after fallback copy insert`，未回退为 spill |
| 2026-03-27 | `待提交` | 禁用 `CALL(B=1,C>=4)` 的 `live-after spill fallback`，改用纯 copy-insert 右移（不做 spill 重写） | `BATTLE_BeforeInitBattle tmp.lua:711 bad argument #1 to insert (table expected, got string)` | 离线日志 `proto19 pc704/727` 由 `live-after spill fallback` 变为 `live-after fallback copy insert`；对应调用窗口不再注入 spill 寄存器链（移除 `MOV A26/A27`），改为纯 `MOV13<-12; MOV12<-11; MOV11<-10` |
| 2026-03-27 | `待提交` | 移除 `CALL(C=3,B=1)` 与 `CALL(C=4,B=1)` 的“param-pass 直接跳过”规则，统一恢复 FR2 参数右移 | `BATTLE_BeforeInitBattle tmp.lua:2556 field or property Equipment does not exist` | 离线反汇编确认 `proto131 line2705/2707` 从 `MOV A4/A5(/A6)` 调整为 `MOV A5/A6(/A7)`；`proto19 line528` 保持 `MOV A13/A14`；`main/migong` 关键点不回退 |
| 2026-03-27 | `待提交` | 收窄 `direct CALL(C=3)` 跳过条件：从“仅 arg1 透传”改为“arg1 + arg2 均为透传 seed 才跳过”；其余场景恢复 FR2 右移 | `BATTLE_BeforeRoleAction tmp.lua:528 bad argument #1 to GetEquipment (Role expected, got number)` | 离线反汇编确认 `proto19 line528` 由 `MOV12/MOV13` 调整为 `MOV13/MOV14`；`proto130 line2571` 同步调整为 `MOV11/MOV12`；`proto131 pc234/240` 保持不变 |
| 2026-03-26 | `待提交` | 收敛 `method-self CALL(C=3)`：由“非 root proto”改为“self/base 回溯到调用者透传 seed 才跳过”；其余 method-self 正常做 FR2 右移 | `migong.lua tmp.lua:29 bad argument #2 to 'byte' (number expected, got table)` | 离线反汇编确认 `proto2 line29` 已从 `MOV16/MOV17` 调整为 `MOV17/MOV18`（参数窗口右移）；battle 关键点 `proto130 pc27/42/107/112` 与 `proto131 pc234/240` 仍保持 |
| 2026-03-26 | `待提交` | 将 `direct CALL(C=2/3)` 跳过条件从 `root proto(pflags=0x03)` 特判改为数据流判定：仅当 `arg1` 源寄存器在调用前“无最近写入”（passthrough seed）才跳过右移 | 去除临时 chunk 特判，定位真正差异 | 离线回归通过；`main proto5 pc111` 维持 FR2 正确形态（`MOV 18<-13`,`MOV 19<-15`,`CALL A16 C3`）；`battle proto130 pc42` 与 `proto131 pc234/240` 关键点保持 |
| 2026-03-26 | `待提交` | 收窄 `direct CALL(C=2/3)`：仅非 root proto(`pflags!=0x03`) 生效；root proto 回退到默认搬移，避免反射链方法调用 self/arg 错槽 | `main.lua tmp.lua:37 bad argument #1 to GetFields (Type expected, got number)` | 静态反汇编已确认 `main proto5 line37` 由 `MOV17/MOV18` 修正为 `MOV18/MOV19`（FR2 正确参数槽）；`line28 ContainsKey` 保持正确；battle 关键点未回退 |
| 2026-03-26 | `待提交` | 收窄 `method-self CALL(C=3)`：仅非 root proto(`pflags!=0x03`) 且 `arg2` 为 `MOV` 时跳过右移；保留 `CALL(C=4,B=1) param-pass` 与 `direct CALL(C=2/3)` 规则 | `main.lua tmp.lua:28 bad argument #1 to ContainsKey (Dictionary expected, got string)` | 静态反汇编已确认 `main proto5 line28` 由 `MOV14/KSTR15` 修正为 `MOV15/KSTR16`（FR2 正确参数槽）；battle 关键点 `proto130(42)/proto131(234,240)` 仍保持正确 |
| 2026-03-26 | `待提交` | 新增 4 条窄规则：`method-self CALL(C=3)`、`direct CALL(C=2)`、`direct CALL(C=3)`、`CALL(C=4,B=1) param-pass` 均跳过 FR2 参数右移，避免 `MOV` 链被整体 +1 后与 `CALL A` 脱节 | `battle.lua` 中 `CheckIfEquipNotRight/GetEquipment/RegisterPrevRole` 相关参数错位 | 静态反汇编已确认 `proto130(pc27/42/124/127)` 与 `proto131(pc224/234/240)` 参数寄存器恢复正确；离线回归通过（`main/battle/migong` 全部 `conversion_failed=0`） |
| 2026-03-22 | `待提交` | 新增 `CALL(C=3)` 窄规则：命中 `func + MOV(arg1) + TGET*(arg2) + CALL` 形态时强制 `copy-fallback`，避免 existing-slice 误保留 FR1 参数布局 | `battle.lua tmp.lua:148 slevels=nil`（`CheckIfSkillUpgraded`） | 离线已验证 `proto132` 两个调用点新增 `MOV11<-10` 与 `MOV10<-9` 搬移链；门禁通过，待真机 |
| 2026-03-22 | `待提交` | `RegisterWorkflowForSkills` 规则扩展：`CALL(C=6/C=4)` 的第二参数来源由 `TDUP` 扩展为 `TDUP|MOV`（root proto），覆盖 `AttackLogic.lua` 中 `KSTR+MOV+FNEW+...` 形态 | `AttackLogic.lua` 报 `RegisterWorkflowForSkills: attempt to concatenate a table value` | 离线反汇编已确认目标段 33 个调用点全部带搬移链（`fail=0`），待真机 |
| 2026-03-22 | `待提交` | 新增 battle 定向门禁：校验 `RegisterWorkflowForSkills` 的 `CALL(C=6/C=4)` 调用点在 FR2 产物中均带正确 MOV 搬移链（防“脚本过、真机仍原错”） | battle `RegisterWorkflowForSkills` 旧错误反复 | 本地门禁通过（`hits=21`，`fail=0`） |
| 2026-03-22 | `待提交` | 在 root proto(`pflags=0x03`) 补回 `CALL(C=4)` 的 `TGETS+KSTR+TDUP+FNEW` 强制 `copy-fallback` | battle `RegisterWorkflowForSkills` 仍报 concat table | 离线反汇编已确认 C4/C6 全部插入 MOV 链，待真机 |
| 2026-03-22 | `待提交` | 收窄 `CALL(C=6)` 的 `KSTR+TDUP+FNEW+KSTR+KPRI` 强制 `copy-fallback`：仅在 root proto(`pflags=0x03`) 生效；避免函数体内大面积误命中 | battle `RegisterWorkflowForSkills` 拼接 table 报错 + 连锁副作用 | 离线通过；`battle copy_fallback_hits` 610→595，`rebuild_count` 4488→839，待真机 |
| 2026-03-22 | `待提交` | 在 `tolua_loadbuffer` 增加构建签名日志：`build=arm64fr2-20260322-rootcall6`，并对 `main/battle/migong` 打印 `conv_ok/conv_fail` | 真机无法确认是否加载最新 so | 已输出可观测标记，待真机回传日志 |
| 2026-03-22 | `cd134e7` | 新增 `CALL(C=6)` 的 `KSTR+TDUP+FNEW+KSTR+KPRI` 强制 `copy-fallback` 路径，规避 `RegisterWorkflowForSkills` 长链调用残留错位 | battle `RegisterWorkflowForSkills` 拼接 table 报错 | 离线转换通过，待真机 |
| 2026-03-22 | `cd134e7` | 更新 `docs/arm64_offline_regression_baseline.json`（battle 指纹变更：`kstr_tdup_rejects=5`、`copy_fallback_hits=240`） | 离线门禁跟随新策略 | 本地门禁已通过 |
| 2026-03-21 | `9aa9e73` | 新增 `tools/run_arm64_offline_regression.ps1` + `docs/arm64_offline_regression_baseline.json`，实现离线门禁（重编调试转换器、批量转换 main/battle/migong、日志指纹比对） | 降低每次改动都上真机的频率 | 本地脚本已跑通，可用于自主回归 |
| 2026-03-21 | `5a9ceaa` | 新增 `tools/check_arm64_fr2_log.ps1`，强制校验 `tolua.c/arm64 so` 改动必须同步更新日志 | 防止“改代码不记账”导致循环回归 | 已启用，本地检查通过 |
| 2026-03-21 | `5a9ceaa` | 新增两条窄规则：`MOV+MOV+TGET*+CALL(C=3)` 的 table-style 两参调用防错位；`KSTR+TDUP` 识别 `new_first` 误绑 `old_second` | `PatchMigong insert` 与 `RegisterWorkflowForSkills` 参数错位 | 本地反汇编已验证目标点修正，待真机 |
| 2026-03-21 | `bbb3af3` | 用 NDK r21e 重编 `arm64-v8a/libtolua.so` 并同步到插件目录 | 同步最新转换逻辑到真机 | 已发布，需结合真机日志判断 |
| 2026-03-21 | `c1ca748` | 收窄两参数 MOV 链规则，仅保留低寄存器形态（old src=0 且 old2 src=1/3）；移除过宽规则（`dense arg block`、`stale new-last`） | 避免“修 Log 时误伤其它调用链” | 部分回落，但仍有 battle/migong 两个主线错误 |
| 2026-03-21 | `b27ec3c` | 强制两参数立即调用准备路径走 repack（battle hooks） | battle 某些参数错位 | 修复局部，同时引入后续回归风险 |
| 2026-03-20 | `759fa45` | 修 ContainsKey 方法风格调用 self/key 参数顺序 | main.lua ContainsKey 参数类型错误 | 已改善 |
| 2026-03-20 | `6c477cb` | 避免 single-arg 调用复用 stale existing-slice | floor/单参调用错位 | 已改善 |

## 3. 回归矩阵（只保留高价值检查）

| 模块 | 关键点 | 预期 | 当前状态 |
|---|---|---|---|
| `main.lua` | `ContainsKey` 调用 | 参数1应为 Dictionary 实例 | 通过（已从主报错列表移除） |
| `migong.lua` | `split` 中 `table.insert` | 第1参数应为 table | 通过（旧问题已修） |
| `migong.lua` | `PatchMigong` 内 `table.insert` | 第1参数应为 table | 本地反汇编通过（`MOV 8/9`），待真机 |
| `battle.lua` | `CheckIfEquipNotRight` 调用 | 参数顺序正确 | 通过（已修） |
| `battle.lua` | `RegisterWorkflowForSkills` | 字符串拼接参与者应为 string | 已对 root proto 的 C6/C4 调用统一加搬移链，并加定向门禁，待真机 |

## 4. 操作约束（避免“拆东墙补西墙”）

1. 每次只改一种规则族（例如仅改 existing-slice 对齐判断），禁止同一提交混入多类策略改动。  
2. 改动后至少核对上表 5 个关键点，再发插件给真机。  
3. 新增规则必须写“命中形态”，避免泛化条件（例如无上限窗口、无 source 约束）。  
4. 真机出现新错误时，先回写本文件“当前主线问题”，再继续改代码。  
5. 若连续 2 次提交都引入新主线回归，下一次必须先做“规则收窄/回退”而非继续加新规则。  

## 5. 本轮后续计划（进行中）

1. 真机回归 `battle.lua/RegisterWorkflowForSkills`（优先验证当前主线）。  
2. 若仍报错，记录首条栈并补充“对应 proto/pc”后再改规则。  
3. battle 通过后再扩展到其它模块回归。  

## 6. 错误指纹台账（真机）

| 日期 | 构建/Commit | 入口 | 首条报错指纹 | 归类 | 处理状态 |
|---|---|---|---|---|---|
| 2026-03-22 | `待提交` | `jygame/AttackLogic.lua` | `RegisterWorkflowForSkills: attempt to concatenate a table value` | call6 规则漏匹配 `MOV` 作为第二参数来源 | 已扩展 `TDUP|MOV` 并重编 so，待真机 |
| 2026-03-22 | `待提交` | `jygame/battle.lua` | `RegisterWorkflowForSkills: attempt to concatenate a table value`（仍复现） | call6 规则命中过宽（函数体链式调用被误改） | 已收窄到 root proto 并重编 so，待真机 |
| 2026-03-22 | `cd134e7` | `jygame/battle.lua` | `RegisterWorkflowForSkills: attempt to concatenate a table value` | 长链 `CALL(C=6)` 参数重排残留 | 已加 force copy-fallback，待真机 |
| 2026-03-21 | `5a9ceaa` | `jygame/battle.lua` | `RegisterWorkflowForSkills: attempt to concatenate a table value` | FR2 参数错位（KSTR+TDUP 邻位） | 本地反汇编已修，待真机 |
| 2026-03-21 | `5a9ceaa` | `jygame/migong.lua` | `PatchMigong: bad argument #1 to 'insert' (table expected, got string)` | table-style 两参调用错位 | 本地反汇编已修，待真机 |

## 7. 提交前检查清单（必须）

1. 执行：`powershell -ExecutionPolicy Bypass -File .\tools\check_arm64_fr2_log.ps1`。
2. 执行：`powershell -ExecutionPolicy Bypass -File .\tools\run_arm64_offline_regression.ps1`。
3. 若输出失败，先补日志或修规则再提交，不允许跳过。
4、 必须确保已报告的错误被解决或减少，不允许没有实际进步提交真机测试。
5. 每条“代码规则变更”必须带三项证据：命中形态、离线日志指纹、真机结论。
6. 真机出现新错误时，只记录首条报错指纹，不做“批量猜测式修复”。

## 8. 记录模板（复制一行到第 2 节）

| YYYY-MM-DD | `commit/待提交` | 规则改动一句话 + 命中形态 | 对应报错指纹 | 反汇编/真机结论 |

| 2026-05-03 | 待提交 | 修正 proto22 的 CALLM(C=1)+前置 open CALL 链在 FR2 下的参数孔位：保留 CALLM 语义不降级，新增 proto22 定向后处理（局部窗口寄存器右移）以匹配 `table.insert(certains, table.remove(argvs,1))` 形态；同时补强离线 `bcrun_cli` 的 `JyGame.Trigger` stub | 用户持续报 `BATTLE_BeforeInitBattle -> CheckTriggerAllowed -> tmp.lua:711 bad argument #1 to insert (table expected, got string)`，并要求先离线验证有效 | 新离线证据：`bcrun_cli_wsl GameEngine.fix6.fr2.bin checktrigger` 通过（不再报 line711）；`bcrun_cli_wsl battle.fix6.fr2.bin getrrevrole` 通过；`check_fr1_callc2_tdup(64/137/2765)` 与 `check_fr2_call_frame_shift(601)` 均通过；`run_arm64_offline_regression` 通过；so 不提交 |

| 2026-05-04 | 待提交 | 修复 GameEngine proto22 中 CALLM(C=1)+前置 open CALL 的 FR2 参数孔位错配：保留 CALLM 语义并增加 proto22 局部后处理，对该链路做最小窗口寄存器右移；同时补齐 `tools/bcrun_cli.c` 对 `JyGame.Trigger` 的离线 stub | 用户持续反馈 battle 初始化链路仍报 `CheckTriggerAllowed/insert` 同类错误，且 APK 曾混入旧插件 | 离线复现与回归：`bcrun_cli_wsl GameEngine.cur.fr2.bin checktrigger` 通过（不再报 line711/line735 insert），`run_arm64_offline_regression` 通过，battle 门禁（64/137/2765、601）和 `getrrevrole` 通过；重编 arm64 so 后哈希 `87AA2DD4E71B1CEC1C87F579C9604624DCB421D00D63AD465248F048661930A2`；so 不提交 |

| 2026-05-05 | 待提交 | 收窄 `CALL(C=3,B=1)` cat-tail 例外：仅对 proto241 中两组 `MOV self<-A3; TGETS func<-A3[23]; ... CAT ...; CALL` 详细形态取消 fast-path skip，让既有 copy-fallback 生成 FR2 method-self 空槽 | 真机报 `AttackLogic_extendTalents -> tmp.lua:4310 bad argument #1 to Log (JyGame.BattleField expected, got string)` | 离线：`run_arm64_offline_regression` 通过；proto241 的 `line3904`/`line4310` 反汇编已变为 `MOV A16/A15/A13; CALL A13` 和 `MOV A23/A22/A20; CALL A20`；so SHA256 `5B729BEA523AC0D6724F37C6E8C719B112FA1F77883D95451ECF9CC59DCDCD3A`；so 不提交 |
